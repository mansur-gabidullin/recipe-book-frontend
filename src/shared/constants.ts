export const LOGIN_PAGE_ROUTE = "/auth/login";

export const AUTH_REDIRECT_LOCATION_STORAGE_KEY = "recipe-book/auth-redirect-location";

export const accessTokenQueryKey = ["accessToken"];

export const CSRF_TOKEN_HEADER_KEY = "x-csrf-token"; // must be in lower case

export const DEFAULT_FAILURE_COUNT = 3;

export const noop = () => undefined;
